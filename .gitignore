# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Design references, not site assets. 161 MB between them, and they used to sit
# in the Vite public directory — so every build copied them into dist/ and a
# deploy would have published the lot. They stay on the machines that have them;
# the images the site actually shows come from Cloudinary through the API.
public/RajdhaniWebsiteReferenceImages/
public/RajdhaniPagesRequireImages/
